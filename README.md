# scrapy_challenge
This little spider is just to get the information related a particular article in amazon web site.

#Command to run the spider
scrapy crawl stack -o items.json -t json

Note: Run it on base directory

After the spider runs it will create a text file (item.txt) with the extracted datails in base directory.
